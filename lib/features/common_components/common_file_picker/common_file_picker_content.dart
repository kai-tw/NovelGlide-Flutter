import 'package:flutter/material.dart';

class CommonFilePickerContent extends StatelessWidget {
  const CommonFilePickerContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Pick File!");
  }
}